# 🚀 Деплой на Render.com

Пошаговая инструкция по размещению сайта в интернете.

---

## 📋 Содержание

1. [Подготовка проекта](#1-подготовка-проекта)
2. [Создание репозитория на GitHub](#2-создание-репозитория-на-github)
3. [Загрузка кода на GitHub](#3-загрузка-кода-на-github)
4. [Деплой на Render.com](#4-деплой-на-rendercom)
5. [Готово!](#5-готово)

---

## 1. Подготовка проекта

### ✅ Проверьте файлы

Убедитесь, что у вас есть эти файлы в корне проекта:
- ✅ `package.json` (есть)
- ✅ `vite.config.ts` (есть)
- ✅ `.gitignore` (создан)
- ✅ `public/photos/` с вашими фотографиями
- ✅ `public/music/background.mp3` с музыкой

### 📸 Добавьте свои фотографии и музыку

**ВАЖНО:** Перед деплоем добавьте ваши файлы:

1. Положите фотографии в `public/photos/` (photo1.jpg, photo2.jpg...)
2. Положите музыку в `public/music/background.mp3`
3. Обновите список фото в `src/app/App.tsx`

---

## 2. Создание репозитория на GitHub

### Шаг 1: Зарегистрируйтесь на GitHub (если еще нет аккаунта)

1. Перейдите на https://github.com
2. Нажмите **Sign up**
3. Заполните форму регистрации
4. Подтвердите email

### Шаг 2: Создайте новый репозиторий

1. Войдите в свой аккаунт GitHub
2. Нажмите **+** в правом верхнем углу
3. Выберите **New repository**
4. Заполните форму:
   - **Repository name**: `family-photo-album` (или любое другое имя)
   - **Description**: "Семейный фотоальбом про Ванюшку"
   - **Public** или **Private** (выберите по желанию)
   - ❌ **НЕ** ставьте галочки на "Add README", "Add .gitignore", "Choose a license"
5. Нажмите **Create repository**

### 🔑 GitHub покажет вам команды - сохраните их!

Пример:
```bash
git remote add origin https://github.com/ваш-username/family-photo-album.git
git branch -M main
git push -u origin main
```

---

## 3. Загрузка кода на GitHub

### Шаг 1: Установите Git (если еще не установлен)

**Windows:**
- Скачайте с https://git-scm.com/download/win
- Установите с настройками по умолчанию

**Mac:**
```bash
brew install git
```

**Linux:**
```bash
sudo apt-get install git
```

### Шаг 2: Инициализируйте Git в вашем проекте

Откройте терминал в папке проекта (в PyCharm: Alt+F12) и выполните:

```bash
# Инициализация Git
git init

# Настройте свое имя и email (один раз)
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"
```

### Шаг 3: Добавьте файлы в Git

```bash
# Добавить все файлы в Git
git add .

# Создать первый коммит
git commit -m "Initial commit: семейный фотоальбом про Ванюшку"
```

### Шаг 4: Загрузите на GitHub

**Замените URL на ваш** (который вы скопировали на GitHub):

```bash
# Добавить удаленный репозиторий
git remote add origin https://github.com/ваш-username/family-photo-album.git

# Переименовать ветку в main
git branch -M main

# Отправить код на GitHub
git push -u origin main
```

### 🔐 Авторизация на GitHub

При первой загрузке GitHub попросит авторизацию:

**Вариант 1: GitHub CLI (рекомендуется)**
- Следуйте инструкциям в терминале
- Авторизуйтесь через браузер

**Вариант 2: Personal Access Token**
1. Перейдите: GitHub → Settings → Developer settings → Personal access tokens
2. Generate new token
3. Скопируйте токен и используйте вместо пароля

### ✅ Проверьте

Откройте ваш репозиторий на GitHub - там должны появиться все файлы!

---

## 4. Деплой на Render.com

### Шаг 1: Зарегистрируйтесь на Render

1. Перейдите на https://render.com
2. Нажмите **Get Started**
3. Зарегистрируйтесь через GitHub (Sign up with GitHub)
4. Разрешите Render доступ к вашим репозиториям

### Шаг 2: Создайте новый Static Site

1. В панели Render нажмите **New** → **Static Site**
2. Найдите ваш репозиторий `family-photo-album`
3. Нажмите **Connect**

### Шаг 3: Настройте проект

Заполните форму:

**Basic Information:**
- **Name**: `family-photo-album` (или любое имя - это будет в URL)
- **Region**: `Frankfurt (EU Central)` (выберите ближайший к вам)
- **Branch**: `main`

**Build & Deploy Settings:**
- **Root Directory**: оставьте пустым
- **Build Command**: 
  ```bash
  npm install && npm run build
  ```
- **Publish Directory**: 
  ```bash
  dist
  ```

**Advanced (необязательно):**
- **Auto-Deploy**: `Yes` (автоматический деплой при изменениях)

### Шаг 4: Создайте сайт

1. Нажмите **Create Static Site**
2. Дождитесь завершения деплоя (обычно 2-5 минут)
3. Статус изменится на **Live** 🎉

### 🌐 Ваш сайт готов!

URL будет выглядеть так:
```
https://family-photo-album.onrender.com
```

(Render предоставляет бесплатный поддомен)

---

## 5. Готово!

### ✅ Ваш сайт в интернете!

Теперь вы можете:
- Открыть сайт по URL: `https://ваше-имя.onrender.com`
- Поделиться ссылкой с друзьями и семьей
- Открыть с любого устройства

---

## 🔄 Обновление сайта

Когда вы захотите добавить новые фотографии или изменить сайт:

```bash
# 1. Внесите изменения в проект (добавьте фото, измените код)

# 2. Добавьте изменения в Git
git add .
git commit -m "Добавлены новые фотографии"

# 3. Отправьте на GitHub
git push

# 4. Render автоматически обновит сайт! (если включен Auto-Deploy)
```

---

## 💡 Полезные команды Git

```bash
# Проверить статус
git status

# Посмотреть историю коммитов
git log

# Добавить конкретный файл
git add путь/к/файлу.jpg

# Отменить изменения (до коммита)
git checkout -- путь/к/файлу

# Посмотреть удаленные репозитории
git remote -v
```

---

## 🆘 Решение проблем

### ❌ Git не распознается как команда

**Решение:** Установите Git с https://git-scm.com/downloads

### ❌ Ошибка при git push

**Решение:** 
1. Проверьте правильность URL: `git remote -v`
2. Авторизуйтесь на GitHub
3. Используйте Personal Access Token вместо пароля

### ❌ Render показывает ошибку билда

**Решение:**
1. Проверьте, что в `package.json` есть скрипт `"build": "vite build"`
2. Убедитесь, что `Build Command` правильная: `npm install && npm run build`
3. Проверьте логи деплоя на наличие ошибок

### ❌ Сайт не загружает фотографии/музыку

**Решение:**
1. Убедитесь, что файлы есть в папке `public/`
2. Проверьте, что файлы загружены на GitHub (откройте репозиторий)
3. Проверьте пути в `src/app/App.tsx`

### ❌ Музыка не воспроизводится

**Это нормально** - браузеры блокируют автовоспроизведение. Теперь будет показываться красивое окно с кнопкой "Включить музыку".

---

## 🎨 Дополнительные возможности Render

### Свой домен

Если у вас есть свой домен (например, `vanyushka.com`):
1. Перейдите в Settings вашего сайта на Render
2. Custom Domains → Add Custom Domain
3. Следуйте инструкциям Render по настройке DNS

### HTTPS

Render автоматически предоставляет бесплатный SSL-сертификат (HTTPS) ✅

---

## 📊 Полный процесс (краткая версия)

```bash
# 1. Подготовка
# - Добавить фото в public/photos/
# - Добавить музыку в public/music/background.mp3

# 2. Git
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/username/repo.git
git push -u origin main

# 3. Render
# - Создать Static Site
# - Подключить GitHub репозиторий
# - Build: npm install && npm run build
# - Publish: dist
# - Create Static Site

# 4. Готово! 🎉
```

---

## ❤️ Готово!

Теперь ваш семейный фотоальбом про Ванюшку доступен всему миру! 🌍👶

**Поделитесь ссылкой с семьей и друзьями!**

---

## 🔗 Полезные ссылки

- GitHub: https://github.com
- Render: https://render.com
- Git документация: https://git-scm.com/doc
- Render документация: https://render.com/docs

---

**Если возникнут вопросы - пишите!** 💬
