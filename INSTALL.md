# 🚀 ИНСТРУКЦИЯ ПО ЗАПУСКУ

## Быстрый старт (рекомендуется)

### Windows:
1. Дважды кликните на файл `start.bat`
2. Дождитесь установки зависимостей и запуска сервера
3. Откройте браузер по адресу http://localhost:5173

### Linux/Mac:
1. Откройте терминал в папке проекта
2. Сделайте скрипт исполняемым: `chmod +x start.sh`
3. Запустите: `./start.sh`
4. Откройте браузер по адресу http://localhost:5173

---

## Ручной запуск (альтернатива)

### 1. Установка Node.js
Если Node.js не установлен:
- Скачайте с https://nodejs.org/
- Установите LTS версию (рекомендуется)
- Проверьте установку в терминале: `node --version`

### 2. Установка зависимостей
Откройте терминал в папке проекта (в PyCharm: Alt+F12) и выполните:

```bash
npm install
```

Эта команда установит все библиотеки:
- React и React DOM
- TypeScript
- Vite (сборщик)
- Tailwind CSS v4
- Motion (анимации)
- Lucide React (иконки)
- И другие зависимости из package.json

### 3. Запуск сервера разработки
После установки выполните:

```bash
npm run dev
```

### 4. Открытие в браузере
Перейдите по адресу, который появится в терминале (обычно http://localhost:5173)

---

## 🎵 Добавление фоновой музыки

### Автоматическое воспроизведение (рекомендуется):

1. Создайте папку `public` в корне проекта (если её нет)
2. Положите ваш аудиофайл в папку `public`
3. Переименуйте файл в `family-music.mp3`
4. При запуске сайта музыка начнет играть автоматически

Структура должна быть такой:
```
family-photo/
├── public/
│   └── family-music.mp3  ← Ваш музыкальный файл здесь
├── src/
├── package.json
└── ...
```

**Важно:** Некоторые браузеры блокируют автоматическое воспроизведение. В этом случае нужно будет нажать кнопку Play один раз.

### Загрузка через интерфейс:

1. Запустите сайт
2. Нажмите иконку настроек ⚙️ (слева сверху)
3. Перейдите на вкладку "Музыка"
4. Загрузите аудиофайл
5. **Примечание:** Музыка сохранится только до закрытия браузера

---

## 📸 Загрузка фотографий

1. Нажмите иконку настроек ⚙️
2. Вкладка "Фото" → нажмите на область загрузки
3. Выберите одну или несколько фотографий
4. Редактируйте подписи по желанию
5. Фотографии сохраняются в localStorage браузера

---

## ⚙️ Команды для разработки

```bash
# Установка зависимостей
npm install

# Запуск сервера разработки
npm run dev

# Сборка для продакшена
npm run build

# Предварительный просмотр сборки
npm run preview
```

---

## 🛠️ Решение проблем

### Ошибки импорта (красные подчёркивания в PyCharm)
1. Убедитесь, что выполнили `npm install`
2. Перезапустите PyCharm
3. В PyCharm: File → Invalidate Caches → Invalidate and Restart

### Порт занят
Если порт 5173 занят, Vite автоматически выберет другой порт (5174, 5175 и т.д.)

### Музыка не воспроизводится автоматически
- Современные браузеры блокируют автоматическое воспроизведение
- Просто нажмите кнопку Play после загрузки страницы
- Или взаимодействуйте со страницей перед воспроизведением

### Node.js не распознается
- Перезапустите терминал/PyCharm после установки Node.js
- Убедитесь, что Node.js добавлен в PATH

---

## 📦 Установленные библиотеки

Проект использует следующие основные библиотеки:

- **React 18.3.1** - UI библиотека
- **TypeScript** - типизация
- **Vite** - быстрый сборщик
- **Tailwind CSS v4** - стилизация
- **Motion (Framer Motion)** - анимации
- **Lucide React** - иконки
- **Radix UI** - компоненты интерфейса

Все зависимости перечислены в `package.json` и устанавливаются командой `npm install`.

---

## ✅ Проверка установки

После выполнения `npm install` проверьте, что:
1. Появилась папка `node_modules` (большая папка с библиотеками)
2. Появился файл `package-lock.json`
3. Нет ошибок в терминале

Теперь можете запустить `npm run dev`!

---

## 💡 Советы

- Используйте Chrome или Firefox для лучшей совместимости
- Для мобильной версии откройте сайт на телефоне через локальную сеть
- Сохраняйте важные фото отдельно - localStorage может быть очищен
- Используйте качественные фото (но не слишком большие файлы)

---

## ❤️ Приятного использования!

Если возникли вопросы - проверьте README.md или напишите в поддержку.
